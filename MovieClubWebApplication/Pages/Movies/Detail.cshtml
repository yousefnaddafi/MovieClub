@page
@model MovieClubWebApplication.Pages.Movies.DetailModel
@{

    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Existing List</h4>
    <hr />  
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.movieDetails.Id)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.movieDetails.Id)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.movieDetails.Title)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.movieDetails.Title)
            </dd>
        </dl>

        <div>
            <a asp-page="./Edit" asp-route-id="@Model.movieDetails.Id">Edit</a> |
            <a asp-page="./Index">Back to List</a>|
            <a asp-page="./Delete" asp-route-id="@Model.movieDetails.Id">Delete</a>
   
</div>

